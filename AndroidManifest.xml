<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.book.now.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="event"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.book.now.app.UpdateActivity"
            android:parentActivityName="com.book.now.app.MainActivity"/>
        <activity
            android:name="com.book.now.app.AddActivity"
            android:label="Add Event"
            android:parentActivityName="com.book.now.app.MainActivity" />
        <activity
            android:name="com.book.now.app.MainActivity"
            android:label="Event Booking">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>